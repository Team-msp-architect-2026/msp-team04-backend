apiVersion: v1
kind: Service
metadata:
  name: moment-backend
  namespace: default
spec:
  selector:
    app: moment-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP